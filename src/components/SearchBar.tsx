import { Search } from 'lucide-react'
import React from 'react'

const SearchBar = () => {
  return (
    <div>
      <Search className='h-5 w-5 hover:text-green-400 hoverEffect' />
    </div>
  )
}

export default SearchBar
